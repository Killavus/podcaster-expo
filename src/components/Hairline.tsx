import React from "react"
import { View } from "dripsy"
import { StyleSheet } from "react-native"

const Hairline = () => (
  <View sx={{ height: StyleSheet.hairlineWidth, backgroundColor: "$muted" }} />
)

export default Hairline
